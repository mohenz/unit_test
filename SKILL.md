---
name: unit-test
description: 워크스페이스 프로젝트의 코드와 요구사항을 분석하여 결함을 식별하고, DEV_TS_0003 규격의 단위테스트 결과서를 자동으로 생성하는 스킬입니다.
---

# 🤖 AI 에이전트 단위테스트 생성 스킬 (Agentic Unit Test Skill)

이 문서는 AI 에이전트가 `unit_test` 표준에 따라 단위테스트를 수행하기 위한 프롬프트 가이드와 제약 사항을 정의합니다.

---

## 🎯 스킬 목표
워크스페이스 프로젝트의 코드와 요구사항을 분석하여, 결함을 식별하고 `DEV_TS_0003` 규격의 결과 산출물을 자동으로 생성하는 능력을 발휘합니다.

---

## 🔄 표준 수행 워크플로우

### 1단계: 테스트 대상 분석 (Analysis) 🔍
- 관련 HTML, JS, CSS 파일의 구조 분석
- 데이터 흐름(API 연동, State 관리) 파악
- UI/UX 설계 요건 확인 (반응형, 컬러 가이드 등)

### 2단계: 테스트 케이스 도출 (Test Case Planning) 📋
- **정상 케이스**: 기본 기능이 의도대로 동작하는가?
- **예외/경계 케이스**: 잘못된 입력, 네트워크 오류 등에 적절히 대응하는가?
- **UI/UX 케이스**: 디자인 가이드를 준수하며 반응형 레이아웃이 깨지지 않는가?
- 최소 5개 이상의 TC를 도출할 것

### 3단계: 테스트 수행 및 증빙 채득 (Execution & Evidence) 🛠️
- 각 케이스별 실행 결과 확인 (Pass/Fail 판정)
- 결함 발견 시 상세 현상 기록 및 재현 경로 파악
- 증빙 자료 수집: 화면 캡처, API Request/Response Payload(JSON), 콘솔 로그

### 4단계: 결과서 작성 및 리포팅 (Reporting) 📝
- `DEV_TS_0003` 템플릿(Markdown) 작성
- 공통 체크리스트 항목 판정 기록
- 증빙 자료(이미지, 코드 블록) 첨부
- **결과물 저장**: 아래 포맷 중 하나 또는 둘 다 생성
  - Markdown: `unit_test/reports/DEV_TS_0003_단위테스트_결과서_[기능명]_[날짜].md`
  - CSV: `unit_test/reports/DEV_TS_0003_단위테스트_결과서_[기능명]_[날짜].csv`

---

## 📏 판정 기준 및 공통 규칙

### 판정 기호
- **✅ Pass**: TC의 예상 결과와 실제 결과가 완전히 일치
- **❌ Fail**: 예상 결과와 다르거나 런타임 오류 발생 → 결함 설명 및 재현 로그 필수 + 수정 코드 제안
- **➖ N/A**: 현재 환경에서 수행 불가 → 사유 명시 필수

### 필수 규칙
1. **객관성 유지**: 실제 실행 결과와 예상 결과를 명확히 구분한다.
2. **증빙 중심**: "정상 동작함" 대신 "API 응답 201 수신 및 DB 적재 확인(증빙 3-1 참조)"과 같이 구체적으로 기술한다.
3. **결함 보고**: `❌ Fail` 판정 시 대안 코드나 해결 방안을 함께 제시한다.
4. **반응형 체크**: 최소 2종(Desktop 1920px, Mobile 375px)에서 레이아웃 렌더링을 확인한다.

---

## ✅ 공통 체크리스트 (필수 적용)

### UI/UX 디자인 & 레이아웃
| 코드 | 점검 항목 | 상세 내용 |
| :--- | :--- | :--- |
| UI-01 | 필수 입력 필드 가시성 | 필수 입력 요소에 `*` 마킹 또는 색상 구분이 되어 있는가? |
| UI-02 | 반응형 렌더링 | 화면 너비 변화 시 컨텐츠가 겹치거나 잘리지 않고 적절히 조정되는가? (375px~1920px) |
| UI-03 | 폰트 및 컬러 일관성 | 정의서에서 지정한 폰트 패밀리 및 색상을 따르는가? |
| UI-04 | 접근성(A11y) | 버튼 및 입력 필드에 적합한 `aria-label` 또는 `alt` 속성이 있는가? |

### 유효성 검사 & 데이터
| 코드 | 점검 항목 | 상세 내용 |
| :--- | :--- | :--- |
| VAL-01 | 입력 데이터 정합성 | 필드 타입에 맞는 값만 입력 가능한가? |
| VAL-02 | 최대/최소 길이 제한 | 텍스트 입력 시 글자수 제한이 시스템적으로 동작하는가? |
| VAL-03 | 빈값(Null) 처리 | 필수값 미입력 후 저장 시도 시 경고 메시지 노출 및 차단되는가? |
| VAL-04 | 중복 데이터 방어 | 유니크해야 하는 정보의 중복 입력을 차단하는가? |

### 기능 & 인터랙션
| 코드 | 점검 항목 | 상세 내용 |
| :--- | :--- | :--- |
| FUN-01 | 기본 동작 무결성 | 버튼 클릭, 링크 이동 등 핵심 UI 인터랙션이 오류 없이 실행되는가? |
| FUN-02 | 로딩 상태 표시 | 데이터 통신 시 로딩 스피너/상태바가 노출되는가? |
| FUN-03 | 에러 핸들링 | API 호출 실패 시 사용자에게 친화적인 에러 메시지를 제공하는가? |
| FUN-04 | 브라우저 히스토리 | 모달 오픈/클로즈 또는 페이지 이동 시 뒤로가기 버튼이 자연스럽게 작동하는가? |

### 보안 & 성능
| 코드 | 점검 항목 | 상세 내용 |
| :--- | :--- | :--- |
| SEC-01 | 인증 권한 체크 | 비로그인 사용자나 권한 없는 기능에 대한 접근이 차단되는가? |
| SEC-02 | XSS/Injection 방어 | 스크립트 코드 입력 시 필터링되는가? |
| PERF-01 | 리소스 최적화 | 이미지나 캡처 데이터가 과도하게 클 경우 압축이 수행되는가? |

---

## 📝 통합 실행 프롬프트 (Core Prompt)

```markdown
# Role: 전문 QA 엔지니어 및 단위테스트 자동화 에이전트
# Task: [기능명]에 대한 단위테스트 수행 및 결과서(DEV_TS_0003) 생성

1. [분석] 해당 기능과 관련된 파일을 모두 읽고 데이터 흐름을 먼저 파악하라.
2. [케이스] 기능적 동작, 유효성 검사, UI/UX(반응형 포함) 관점의 TC를 정의하라.
3. [수행] 브라우저 및 로그를 통해 각 TC를 검증하고 Pass/Fail을 판정하라.
4. [산출] unit_test/reports/ 폴더 내에 마크다운 형식의 결과서를 작성하여 저장하라.
5. [완료] 수행 과정에서 발견된 주요 결함과 개선 권고 사항을 요약 보고하라.
```

---

## 📁 산출물 파일명 규칙

```
# Markdown
DEV_TS_0003_단위테스트_결과서_[파일명/기능명]_[날짜].md

# CSV
DEV_TS_0003_단위테스트_결과서_[파일명/기능명]_[날짜].csv

저장 위치: unit_test/reports/
```

---

## 📊 CSV 출력 포맷

사용자가 CSV 포맷을 요청하는 경우, 아래 컬럼 구조로 결과서를 생성합니다.

### CSV 컬럼 정의

| 컬럼명(헤더) | 설명 | 예시 |
| :--- | :--- | :--- |
| `유형` | 테스트 케이스 유형 분류 (UI / VAL / FUN / SEC / PERF) | `UI` |
| `순번` | 케이스 일련번호 | `1` |
| `케이스 명` | 테스트 케이스 이름 | `반응형 렌더링 확인` |
| `내용` | 테스트 수행 상세 내용 | `375px~1920px 화면에서 레이아웃이 깨지지 않는지 확인` |
| `테스트 데이터` | 테스트에 사용한 입력값 또는 조건 | `화면 너비 375px, 1920px` |
| `예상결과` | 기대되는 동작 또는 출력 결과 | `컨텐츠가 겹치거나 잘리지 않고 정상 표시됨` |
| `참고이미지` | 관련 캡처 이미지 파일명 또는 경로 | `evidence_TC01_375px.png` |
| `실제결과 (제외사유)` | 실제 수행 결과, N/A인 경우 제외 사유 기재 | `375px에서 버튼 겹침 발생 / N/A: 해당 환경 미구성` |

### CSV 헤더 행 (복사 사용)
```
유형,순번,케이스 명,내용,테스트 데이터,예상결과,참고이미지,실제결과 (제외사유)
```

### CSV 생성 규칙
1. **인코딩**: UTF-8 BOM 포함 (`UTF-8 with BOM`) → 엑셀에서 한글 깨짐 방지
2. **구분자**: 쉼표(`,`) 사용, 값 내 쉼표가 있을 경우 큰따옴표(`"`)로 감쌀 것
3. **첫 행**: 반드시 위 컬럼 헤더 행으로 시작
4. **참고이미지**: 캡처 파일이 있을 경우 파일명 기재, 없으면 빈 값 허용
5. **N/A 행**: `실제결과 (제외사유)` 컬럼에 제외 사유를 명확히 기재할 것
