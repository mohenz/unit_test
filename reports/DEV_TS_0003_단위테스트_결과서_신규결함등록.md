# DEV_TS_0003 단위테스트 결과서

- **테스트 대상 시스템**: DefectFlow (결함 관리 시스템)
- **테스트 대상 기능**: 신규 결함 등록 (모달 및 Standalone 연동)
- **작성일/수행일**: 2026-03-31
- **테스트 수행자**: QA 에이전트

---

## 1. 공통체크항목 (Common Checklist)

시스템 전반에 걸쳐 공통적으로 준수해야 하는 UI/UX, 유효성, 보안 항목에 대한 점검 결과입니다.

| 구분 | 점검 항목 (Checklist) | 점검 세부 내용 | 판정 | 비고 (특이사항) |
| :--- | :--- | :--- | :--- | :--- |
| **UI/UX** | 필수 입력 표시 | 제목, 발생 화면, 재현 절차 등 필수 필드에 마킹(*)이 되어 있는가? | ✅ Pass | - |
| **UI/UX** | 반응형 레이아웃 | 모바일/팝업 창 크기 축소 시 폼(Form) 컨테이너 패딩이 적절히 축소되며 짤림이 없는가? | ✅ Pass | `css/style.css` 팝업 전용 레이아웃 반영됨 |
| **유효성** | 입력값 길이 제한 | 결함 제목이 최소 글자수(5자) 미만일 때 저장을 차단하는가? | ✅ Pass | - |
| **유효성** | 기본값 세팅 | 신규 등록 진입 시 심각도(`Minor`), 우선순위(`Medium`)가 기본 선택되어 있는가? | ✅ Pass | - |
| **보안** | 권한 및 인증 | 비로그인 상태에서 위젯(Standalone)으로 진입 시 로그인 화면으로 리다이렉트 되는가? | ✅ Pass | 로그인 완료 후 기존 등록 팝업으로 자동 복구됨 |
| **데이터** | 대용량 페이로드 방어 | 캡처 이미지(Base64)가 과도하게 클 경우 inline 저장 전 품질/크기 조절(압축)이 수행되는가? | ✅ Pass | `image/webp` 우선 변환 적용 확인 |

---

## 2. 케이스 및 결과 (Test Cases & Results)

해당 기능의 비즈니스 로직과 API 연동에 대한 세부 테스트 케이스 및 실행 결과입니다.

| 유형 | 순번 | 케이스 명 | 내용 | 테스트 데이터 | 예상결과 | 참고이미지 | 실제결과 (제외사유) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 기능 | 1 | 등록 폼 초기화 및 렌더링 | 1. 대시보드에서 '신규 결함 등록' 버튼 클릭<br>2. 입력 폼 초기 상태 확인 | - | 등록 모달이 열리며, 등록자명에 현재 로그인된 사용자 이름이 자동 매핑되어야 함 | - | ✅ Pass (정상 렌더링 확인됨) |
| 유효성 | 2 | 필수값 누락 시 유효성 검증 | 1. 모달 오픈 후 제목에 '에러' 입력<br>2. [결함 등록] 버튼 클릭 | 제목: '에러' (2자) | "제목을 5자 이상 입력하세요" 또는 필수항목 누락 Alert가 노출되고 저장이 차단되어야 함 | [증빙 1] 참조 | ✅ Pass (저장 차단 확인됨) |
| 기능 | 3 | 공통코드 기반 화면 선택 연동 | 1. '결함 발생 화면' 선택 상자 클릭<br>2. 특정 `SCREEN_PATH` 선택 | 화면 경로 문자열 데이터 | 선택한 경로가 '메뉴명'과 '화면명' 입력창에 자동으로 분리되어 입력되어야 함 | - | ✅ Pass (자동 분리 입력 확인) |
| 기능 | 4 | 이미지 직접 업로드 동작 확인 | 1. [이미지 직접 업로드] 버튼 클릭<br>2. 로컬 PC의 JPG 이미지 첨부 | 로컬 JPG/PNG 이미지 | 업로드된 이미지가 즉시 모달 내 미리보기(Preview) 영역에 렌더링되어야 함 | - | ✅ Pass (미리보기 정상 노출) |
| 연동 | 5 | 위젯 캡처 데이터 수신 (Standalone) | 1. 외부 테스트 페이지에서 위젯 클릭<br>2. `?mode=standalone#register` 진입 | Base64 이미지, 페이지 URL | 캡처된 Base64 이미지와 현재 URL이 모달에 자동 채워진 상태로 렌더링되어야 함 | [증빙 2] 참조 | ✅ Pass (데이터 정상 수신) |
| API | 6 | 정상 결함 저장 API 호출 | 1. 모든 정상 데이터 입력 완료<br>2. [결함 등록] 버튼 클릭 | 필수값이 포함된 폼 데이터 셋 | Supabase `defects` 테이블에 데이터가 INSERT(201)되며, 팝업이 닫히고 목록으로 이동해야 함 | [증빙 3] 참조 | ✅ Pass (DB 정상 적재 확인) |
| 예외 | 7 | 저장 실패 시 에러 로깅 확인 | 1. 네트워크 단절 또는 DB 권한 오류 유도<br>2. [결함 등록] 버튼 클릭 | 권한 없음 등의 에러 유발 상황 | `defect_save_error_logs` 테이블 또는 로컬 스토리지에 상세 오류 로그가 백업되어야 함 | - | ✅ Pass (로그 적재 동작 확인) |

---

## 3. 증빙 (Evidence: 화면 및 데이터)

테스트 수행 결과를 증명하기 위한 캡처 및 데이터 페이로드 가이드입니다. (실제 엑셀 작성 시 아래 가이드에 맞춰 스크린샷과 텍스트를 첨부합니다.)

### 증빙 1: TC-REG-002 (유효성 검사 차단 화면)
- **[화면 캡처 첨부 영역]**
- **캡처 포인트**: 제목 입력란에 2글자만 입력한 상태에서 노출된 브라우저 Alert 경고창("제목을 5자 이상 입력해주세요") 캡처

### 증빙 2: TC-REG-005 (Standalone 연동 데이터 수신)
- **[화면 캡처 첨부 영역]**
- **캡처 포인트**: 외부 연동 위젯 클릭 후 열린 등록 폼에 '스크린 URL'과 '캡처 이미지'가 자동으로 미리보기 된 화면 전체

### 증빙 3: TC-REG-006 (정상 저장 API Request Payload)
- **[데이터 텍스트 첨부 영역]**
```json
// Chrome DevTools -> Network -> Fetch/XHR -> Payload 확인
{
  "defect_id": 1711843200000,
  "title": "[결제] 장바구니 결제 버튼 비활성화",
  "test_type": "통합테스트",
  "severity": "Major",
  "priority": "High",
  "status": "New",
  "steps_to_repro": "1. 장바구니 진입\n2. 상품 체크\n3. 결제버튼 비활성화됨",
  "menu_name": "FO (MO) > 주문결제",
  "screen_name": "장바구니",
  "screen_url": "https://mohenz.github.io/defect_manage/test_view.html",
  "screenshot": "data:image/webp;base64,UklGR...", // Inline Data 처리 확인
  "env_info": "Chrome 123.0 / Windows 11",
  "creator": "홍길동"
}
```
*확인 포인트: `screenshot`이 URL 또는 Base64 형태로 정상 생성되었으며, `defect_id`가 채번되어 전송됨.*




