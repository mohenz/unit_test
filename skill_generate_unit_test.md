# 🤖 AI 에이전트 단위테스트 생성 스킬 (Agentic Unit Test Skill)

이 문서는 AI 에이전트가 `unit_test` 표준에 따라 단위테스트를 수행하기 위한 프롬프트 가이드와 제약 사항을 정의합니다.

---

## 🎯 스킬 목표
워크스페이스 프로젝트의 코드와 요구사항을 분석하여, 결함을 식별하고 `DEV_TS_0003` 규격의 결과 산출물을 자동으로 생성하는 능력을 발휘합니다.

## 🛠️ 주요 하위 동작 (Action Steps)

### 1. 테스트 플래닝 (Planning)
- **프롬프트 전략**: "제시된 기능을 검증하기 위해 필요한 정상/예외/UI 시나리오를 최소 5개 이상 도출하라."
- **분석 범위**: HTML 구조(Accessibility), JS 로직(Data Validation), CSS(Responsive Layout).

### 2. 정적 및 동적 분석 (Analysis)
- **정적 분석**: 파일을 읽어 잠재적 버그, 오타, 하드코딩된 변수 등을 식별.
- **동적 분석**: 브라우저 도구를 사용하여 실제 UI 렌더링 상태를 점검하고 API 호출 페이로드를 가로채어 검증.

### 3. 리포팅 (Reporting)
- **포맷**: Markdown 기반 `DEV_TS_0003` 템플릿 사용.
- **판정 준수**: `✅ Pass`, `❌ Fail`, `➖ N/A(해당없음)` 기호 사용.
- **코드 증빙**: JSON 페이로드나 핵심 코드 조각을 `### 증빙` 섹션에 코드 블록으로 필수 포함.

## ⚠️ 제약 사항 및 규칙

1. **객관성 유지**: 실제 실행 결과와 예상 결과를 명확히 구분한다.
2. **증빙 중심**: "정상 동작함"이라는 표현 대신 "API 응답 201 수신 및 DB 적재 확인(증빙 3-1 참조)"과 같이 구체적으로 기술한다.
3. **결함 보고**: `❌ Fail` 판정 시에는 대안 코드나 해결 방안을 함께 제시해야 한다.
4. **반응형 체크**: 최소 2종 이상의 화면 크기(Desktop 1920px, Mobile 375px)에서 레이아웃 렌더링을 확인한다.

---

## 📝 통합 실행 프롬프트 (Core Prompt)

```markdown
# Role: 전문 QA 엔지니어 및 단위테스트 자동화 에이전트
# Task: [기능명]에 대한 단위테스트 수행 및 결과서(DEV_TS_0003) 생성

1. [분석] 해당 기능과 관련된 파일을 모두 읽고 데이터 흐름을 먼저 파악하라.
2. [케이스] 기능적 동작, 유효성 검사, UI/UX(반응형 포함) 관점의 TC를 정의하라.
3. [수행] 브라우저 및 로그를 통해 각 TC를 검증하고 Pass/Fail을 판정하라.
4. [산출] unit_test/ 폴더 내에 마크다운 형식의 결과서를 작성하여 저장하라.
5. [완료] 수행 과정에서 발견된 주요 결함과 개선 권고 사항을 요약 보고하라.
```
